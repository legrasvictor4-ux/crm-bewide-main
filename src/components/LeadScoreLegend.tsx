const LeadScoreLegend = () => (
  <div className="text-xs text-muted-foreground">
    <span className="font-semibold">Lead Score</span> estime la propension à convertir (0–100).
  </div>
);

export default LeadScoreLegend;
